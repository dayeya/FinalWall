from abc import ABC, abstractmethod

class HTTPHandler(ABC):
    """
    Base HTTPHandler class to inherit from.
    Provides special functions to modify.
    """
    def __init__(self) -> None:
        pass